# Автоматически сгенерировано Ansible
# Не редактировать вручную!

DEBUG={{ debug | lower }}
API_KEY={{ vault_api_key | default('default') }}
MAX_MSG={{ max_msg }}
MAX_RATE={{ max_rate }}
ALLOWED_HOSTS={{ allowed_hosts | join(',') }}
DOMAIN={{ domain }}